from .BaseNode2Sides import BaseNode2Sides

class NodeEq(BaseNode2Sides):
    """
    Represents a logical Equivalency operation node.

    This class inherits from `BaseNodeExtended` and is used to model
    the logical Equivalency operation in a tree structure. It does not add
    additional functionality beyond that provided by its base class
    but serves to explicitly represent the Equivalency logical operation.

    Inherits:
        BaseNodeExtended: The extended base class for logical nodes.
    """
    pass