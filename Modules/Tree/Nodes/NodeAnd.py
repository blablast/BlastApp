from .BaseNodeExtended import BaseNodeExtended

class NodeAnd(BaseNodeExtended):
    """
    Represents a logical AND operation node.

    This class inherits from `BaseNodeExtended` and is used to model
    the logical AND operation in a tree structure. It does not add
    additional functionality beyond that provided by its base class
    but serves to explicitly represent the AND logical operation.

    Inherits:
        BaseNodeExtended: The extended base class for logical nodes.
    """
    pass
